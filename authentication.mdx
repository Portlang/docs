---
title: "Authentication"
---

All requests to HS Ping requires an active API Key.

To initiate a request, an active API Key must be included within the HTTP request header:

<kbd>Authorization: Bearer API_KEY</kbd>

## Creating an API Key

You can create an API key through the HS Ping Dashboard → API Keys.